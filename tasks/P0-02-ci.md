---
id: P0-02
title: "CI on pull requests"
phase: 0
lane: shared
status: done
owner: codex
depends_on: [P0-01]
research: []
branch: task/P0-02-ci
---

## Execute in phases
Phase 0, lane shared. Wave and session: see docs/PLAN.md. **Start only after none (start now) is logged as passed and every `depends_on` task is `done`.** Inside the session: read → plan (in the Log) → test first → build → `make check` → hand off (AGENTS.md). Facts for this task are already researched in the docs/research files named in `research:`.

## Goal
GitHub Actions runs `make check` on every PR and on main, with dependency caching.

## Why it matters
Keeps main green without relying on anyone remembering to run tests.

## Files you may touch
- .github/workflows/ci.yml

## Do not touch
- Anything not listed above. Put needed changes under Follow-ups.

## Acceptance criteria
- [x] CI runs on pull_request and push to main
- [x] Installs Chromium and its Linux dependencies for the smoke test
- [x] Finishes in under 8 minutes (hosted PR check completed in 49 seconds)
- [x] Never needs secrets (mock mode)

## Tests to add
- The workflow itself; prove it by a failing test on a throwaway branch

## How to verify (human, under 5 minutes)
1. Run `gh run view 35928683229 --repo mshaaf/microsoftinnovationhackathon`. It should show a passing check completed in 49 seconds.
2. Run `gh run view 35928957960 --repo mshaaf/microsoftinnovationhackathon`. It should show the intentionally failing test failed CI in 46 seconds; its temporary PR and branch were deleted.

## Facts to respect
- (none)

## Log
<!-- Agent appends: date, what was done, last 10 lines of `make check`, open questions. -->

- 2026-09-23: Started as explicitly directed while P0-01 is at review commit `3a3afa6` and not merged; this worktree is a draft based on that commit. Do not push or open a PR until P0-01 merges and this branch is rebased on main.
- Plan:
  1. Add one GitHub Actions workflow for pull requests and pushes to main.
  2. Cache uv, pnpm, and Playwright browser downloads using the scaffold lockfiles.
  3. Install Python 3.12, Node 22, and Chromium; run `make setup` then `make check` in mock mode.
  4. Validate the workflow locally, run `make check`, and record the result and remaining hosted-CI verification.
- 2026-09-23: Added `.github/workflows/ci.yml` with read-only permissions, mock mode, uv/pnpm/Playwright caches, and an 8-minute job timeout. `pnpm exec playwright install --with-deps chromium` includes the Linux runtime libraries needed by smoke tests.
- Local checks: `make setup` passed; `pnpm exec playwright install chromium` downloaded the local browser; `make check` exited 0; workflow YAML parse/assertions and `git diff --check` passed. Contract, fixture, and smoke scripts are scaffold stubs until P0-03, P0-05, and P0-08.
- `make check` last 10 lines:
  ```text
  Test Files  1 passed (1)
       Tests  1 passed (1)
    Start at  18:10:37
    Duration  1.01s (transform 69ms, setup 0ms, collect 179ms, tests 48ms, environment 425ms, prepare 70ms)
  uv run --project backend python scripts/validate_contracts.py
  not implemented yet (P0-03)
  uv run --project backend python scripts/validate_fixtures.py
  not implemented yet (P0-05)
  bash scripts/smoke.sh
  not implemented yet (P0-08)
  ```
- 2026-09-23: P0-01 merged; rebased on `origin/main` at `f10fd59`. Post-rebase `make check` exited 0. Hosted checks can now run.
- Post-rebase `make check` last 10 lines:
  ```text
   Test Files  1 passed (1)
        Tests  1 passed (1)
     Start at  18:29:09
     Duration  2.55s (transform 128ms, setup 0ms, collect 759ms, tests 48ms, environment 1.36s, prepare 95ms)
  uv run --project backend python scripts/validate_contracts.py
  not implemented yet (P0-03)
  uv run --project backend python scripts/validate_fixtures.py
  not implemented yet (P0-05)
  bash scripts/smoke.sh
  not implemented yet (P0-08)
  ```
- Learned: Playwright needs Linux system packages as well as cached browser binaries, so CI installs Chromium with `--with-deps`.
- Hosted verification: PR #2 passed in 49 seconds (run `35928683229`). Temporary failing-test PR #3 failed in 46 seconds (run `35928957960`); PR and branch were closed/deleted afterward.

## Follow-ups
<!-- Changes needed outside this task's files. -->
